{
  "*.cpp": {
    "template": [
      "import std;",
      "",
      "struct Line {",
      "  std::string str;",
      "};",
      "",
      "std::ostream& operator<<(std::ostream& os, const Line& line) {",
      "  return os << \"Line {open} \" << line.str << \" {close}\";",
      "}",
      "",
      "std::istream& operator>>(std::istream& is, Line& line) {",
      "  std::string str;",
      "  if (is >> str) {",
      "    line = {open}str{close};",
      "    return is;",
      "  }",
      "  if (is.eof()) {",
      "    return is;",
      "  }",
      "  throw std::runtime_error(\"failed parsing input\");",
      "}",
      "",
      "int main() {",
      "  std::ios_base::sync_with_stdio(false);",
      "",
      "  const auto lines = std::views::istream<Line>(std::cin) |",
      "                     std::ranges::to<std::vector<Line>>();",
      "  std::ranges::copy(lines, std::ostream_iterator<Line>(std::cout, \"\\n\"));",
      "",
      "  const auto part1{open}0{close};",
      "  const auto part2{open}0{close};",
      "  std::print(\"{open}{close} {open}{close}\\n\", part1, part2);",
      "",
      "  return 0;",
      "}"
    ]
  }
}
